export function levenshteinDistance(e,n){const t=[];for(let e=0;e<=n.length;e++)t[e]=[e];for(let n=0;n<=e.length;n++)t[0][n]=n;for(let i=1;i<=n.length;i++)for(let o=1;o<=e.length;o++)n.charAt(i-1)===e.charAt(o-1)?t[i][o]=t[i-1][o-1]:t[i][o]=Math.min(t[i-1][o-1]+1,t[i][o-1]+1,t[i-1][o]+1);return t[n.length][e.length]}export function findClosestCommand(e,n,t=2){let i=null,o=1/0;for(const s of n){const n=levenshteinDistance(e.toLowerCase(),s.toLowerCase());n<=t&&n<o&&(o=n,i=s)}return i}export function findSimilarCommands(e,n,t=3){return n.map(n=>({command:n,distance:levenshteinDistance(e.toLowerCase(),n.toLowerCase())})).filter(e=>e.distance<=3).sort((e,n)=>e.distance-n.distance).slice(0,t).map(e=>e.command)}export function isSimilar(e,n,t=.8){const i=Math.max(e.length,n.length);return 0===i||1-levenshteinDistance(e,n)/i>=t}export default{levenshteinDistance:levenshteinDistance,findClosestCommand:findClosestCommand,findSimilarCommands:findSimilarCommands,isSimilar:isSimilar};